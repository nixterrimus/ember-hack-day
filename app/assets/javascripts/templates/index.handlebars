<div class="admin_trips span12">
  {{#each trip in controller }}
  <div class="trip">
    <div class="header">{{trip.name}}</div>
    <div class="details">{{trip.description}}</div>
  </div>
  {{/each}}
</div>
