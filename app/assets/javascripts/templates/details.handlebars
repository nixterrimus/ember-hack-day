<h3>{{ selectedTrip.name }}</h3>
<div class="details"><p>{{selectedTrip.description}}<p></div>

<h4>Hotel Options</h4>
<ul>
  {{#each hotel in selectedTrip.hotels }}
  <li>{{ template "hotel_detail" }}</li>
  {{/each}}
</ul>
